name: Cache Workflow

on:
  push:

jobs:
  cache-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: .
          key: cache-5010c89

      - name: prime-cache-5010c89
        run: echo "Cache hit? ${{ steps.cache.outputs.cache-hit }}"
